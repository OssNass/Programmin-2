---
theme: ./slidev-theme-penguin-rtl
colorSchema: light
class: text-center
transition: slide-right
title: "المحاضرة 6: الملفات الجزء الثاني"
mdc: true
author: Dr. Ossama Nasser
exportFilename: 6- الملفات الجزء الثاني
layout: cover
hideInToc: true
---
# المحاضرة 6
## الملفات الجزء الثاني
### د. أسامه ناصر
2025-2026

---

```yaml
hideInToc: true
```
# الفهرس
<toc />

---

# الملفات العشوائية
- الملفات العشوائية تعبر عن آلية قراءة البيانات من الملف
- على خلاف الملفات التسلسلية، التي يتم القراءة فيها بشكل متتالي (بايت مع تاليه)
- تسمح لك الملفات العشوائية بالانتقال على الملف بشكل كيفي
- أي أنها تسمح لنا بالتعامل الفعلي مع الملفات على أنها مصفوفات
- تتم فيها عملية قراءة وكتابة البيانات كمصفوفة بايتات
---

## كيف تخزن الملفات العشوائية البيانات
- إن عملية القراءة والكتابة من الملفات العشوائية لا تتعامل مع البيانات بصفة نصوص كما في الملفات التسلسلية
- أي عند تخزين عدد صحيح (4 بايت) ضمن ملف نصي يتم تخزينه بصفة نص
	- مثلًا لدينا الرقم 23986743 ونريد تخزينه ضمن ملف
	- في الملفات التسلسلية يخزن بصفة نص أي بمعنا آخر يتم تخزين كل خانة على بايت مستقل
	- أي كلما كان الرقم مكون عدد خانات أكبر احتاج عدد بايتات أكبر مع أنّه يخزن ضمن الذاكرة بالاعتماد فقط على 4 بايتات
	- الملفات العشوائية تعمل تخزينه كما هو موجود في الذاكرة
		- أي أنها تأخذ صورة من الذاكرة وتخزنها في ملف (تقريبًا)
---

# التعامل مع الملفات العشوائية ضمن لغة ++C
- للتعامل مع الملفات العشوائية في ++C نلجأ عادةً للصف `fstream`
	- لا يمنعنا شيء من استخدام الصف `ofstream` أو الصف `ifstream`
	- لكننا نعمل عادةً مع الصف `fstream` في الملفات العشوائية
- لفتح الملف نستخدم أحد الشكلين التاليين
	- `fstream file('path',ios::out|ios::in|ios::binary)`
```cpp
	  fstream file;
	  file.open('path',ios::out|ios::in|ios::binary);
```

- التابع `open` أحد توابع الصف fstream يسمح لنا بفتح ملف بعد بناء الصف
---

## أعلام الفتح
- يوجد لدينا في عملية الفتح مجموعة من الأعلام
	- `ios::out` تعني أننا نفتح الملف في وضع الكتابة، يتم هنا الكتابة من بداية الملف
	- `ios::in` نفتح الملف في وضع القراءة
	- `ios::app` فتح في الملف في وضع الكتابة ولكن من نهاية الملف، أي أننا لا نكت من البداية
	- `ios::binary` نفتح الملف في الوضع الثنائي (التعامل معه كسلسلة بايتات وليس سلسلة محارف)
- يمكن دمج هذه الأعلام مع بعضها البعض باستخدام المعامل `|`
	- عملية Or المنطقية تشابه عملية الجمع
--- 

## القراءة والكتابة
- نفترض لدينا السجل التالي:
```cpp
struct Student{
	long id;
	char firstName[20];
	char lastName[20];
	float points;
};
```

- من الملاحظ هنا استخدامنا لمصفوفة محارف بديلةً عن الصف string
- الموضوع له علاقة بتثبيت حجم السجل المقروء والمكتوب للملف
- يمكن كتابة وقراءة سجل ذو حجم متغيير، لكن العملية أصعب من أن نتعامل معها حاليًا، وتتطلب ترتيبات إضافية
- المطلوب قراءة وكتابة سجلات الطلاب عشوائيًا ضمن ملف
---

```yaml
hideInToc: true
```
## القراءة والكتابة
- مؤشر القراءة والكتابة
	- تمتلك الملفات العشوائية مؤشران يستخدمان لتحديد مكان القراءة والكتابة
	- مؤشر القراءة `get` يعبر عن المكان الذي نقرأ منه حاليًا ، لتغيير الموقع نستخدم التابع `seekg` ولمعرفة الموقع نستخدم `tellg`
	- مؤشر الكتابة `put` يعبر عن المكان الذي نكتب إليه حاليًا ، لتغيير الموقع نستخدم التابع `seekp` ولمعرفة الموقع نستخدم `tellp`

<img src="./media/6/pointers.png">

---

```yaml
hideInToc: true
```
## القراءة والكتابة
- بفرض أننا نريد كتابة سجل إلى الملف، نعمل على استخدام التابع `write` كما يلي:
```cpp
file.write((char *)(&v),sizeof(student))
```

- نلاحظ هنا قصر نوع المؤشر الذي يؤشر على السجل من `* student` إلى `* char`
	- يمكننا أيضًا أن نعتمد على العملية 
```cpp
	reinterpret_cast<char*> 
```

- الوسيط الثاني هو حجم السجل لتحديد عدد البايتات التي يتم كتابتها
- بمعنىً آخر نعمل على قراءة مصفوفة بايتات من الملف عدد عناصرها مساوِ لعدد البايتات التي يتطلبها السجل `student` في الذاكرة